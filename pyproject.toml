[project]
name = "diagram"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "flask>=3.0.3",
    "polars>=1.8.2",
    "setuptools>=75.3.2",
    "tree>=0.2.4",
]
